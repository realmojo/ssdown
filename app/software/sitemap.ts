// Software sitemap is served by /sitemap-software.xml (app/sitemap-software.xml/route.ts)
import { MetadataRoute } from "next";
export default function sitemap(): MetadataRoute.Sitemap {
  return [];
}
